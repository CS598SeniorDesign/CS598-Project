# Shell scripts run inside Linux containers and break with CRLF line endings
# ("set: Illegal option -"), so always check them out with LF regardless of core.autocrlf.
*.sh text eol=lf
.husky/* text eol=lf
